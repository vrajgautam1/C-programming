#include <stdio.h>

int main()
{
    //-1
    // int size;
    // printf("enter size: ");
    // scanf("%d", &size);
    // int arr[size];

    // printf("enter array elements:\n");
    // int counter = 0;
    // for (int i = 0; i < size; i++)
    // {
    //     printf("element %d: ", i + 1);
    //     scanf("%d", &arr[i]);
    //     counter++;
    // }

    // printf("size of the array is: %d",counter);

    //-2
    // int size;
    // printf("enter the size of the array: ");
    // scanf("%d", &size);

    // int arr[size];
    // int sum = 0;
    // for (int i = 0; i < size; i++)
    // {
    //     printf("enter no.%d ", i + 1);
    //     scanf("%d", &arr[i]);
    //     sum += arr[i];
    // }

    // float avg = (float)sum/size;
    // printf("average is: %.2f", avg);


    //-3
    // int size;
    // printf("enter the size of the two arrays: ");
    // scanf("%d", &size);

    // int arrA[size];
    // int arrB[size];
    // int arrC[size];

    // for(int i = 0; i < size; i++){
    //     printf("enter Array-A no.%d: ", i+1);
    //     scanf("%d", &arrA[i]);

    //     printf("enter Array-B no.%d: ", i+1);
    //     scanf("%d", &arrB[i]);
    // }

    // for(int i = 0; i < size; i++){
    //     arrC[i] = arrA[i]+arrB[i];
    // }


    // printf("Arr c is: ");
    // for(int i = 0; i < size; i++){
    //     printf("%d ", arrC[i]);
    // }
}
#include<stdio.h>

void main(){
    int size;
    printf("enter the size of the array: %d\n");
    scanf("%d",&size);

    int array1[size];

    printf("enter the elements of the array");

    for(int i=0; i<size; i++){
        scanf("%d",&array1[i]);
    }

    int array2[size];
    

}